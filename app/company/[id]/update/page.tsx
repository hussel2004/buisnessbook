export default function CompanyUpdatePage() {
    return (
        <div>
            <h1>Update Company Page</h1>
            {/* Form to update company details, add a post there CRUD on promotions and posts. */}
        </div>
    );
}